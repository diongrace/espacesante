{% extends 'base.html.twig' %}

{% block title %}Batiment{% endblock %}

{% block body %}
    <h1>Batiment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ batiment.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ batiment.libelle }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ batiment.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mt-b" href="{{ path('app_batiment_index') }}">back to list</a>

    <a class="btn btn-primary mt-b" href="{{ path('app_batiment_edit', {'id': batiment.id}) }}">edit</a>

    {{ include('batiment/_delete_form.html.twig') }}
{% endblock %}
