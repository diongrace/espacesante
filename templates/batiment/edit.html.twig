{% extends 'base.html.twig' %}

{% block title %}Edit Batiment{% endblock %}

{% block body %}
    <h1>Edit Batiment</h1>

    {{ include('batiment/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary mt-b" href="{{ path('app_batiment_index') }}">back to list</a>

    {{ include('batiment/_delete_form.html.twig') }}
{% endblock %}
