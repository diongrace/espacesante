{% extends 'base.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>NomChambre</th>
                <td>{{ chambre.nomChambre }}</td>
            </tr>
            <tr>
                <th>NumeroChambre</th>
                <td>{{ chambre.numeroChambre }}</td>
            </tr>
            <tr>
                <th>NombreLits</th>
                <td>{{ chambre.nombreLits }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary mt-b" href="{{ path('app_chambre_index') }}">back to list</a>

    <a class="btn btn-primary mt-b" href="{{ path('app_chambre_edit', {'id': chambre.id}) }}">edit</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}
