<form method="post" action="{{ path('app_lits_delete', {'id': lit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lit.id) }}">
    <button class="btn btn-danger mt-4">Delete</button>
</form>
