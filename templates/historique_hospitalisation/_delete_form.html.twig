<form method="post" action="{{ path('app_historique_hospitalisation_delete', {'id': historique_hospitalisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historique_hospitalisation.id) }}">
    <button class="btn btn-danger mt-4">Delete</button>
</form>
